$('#myTripsModal').modal('hide');
$('#tripInfoModal .modal-body').html("<%= j render 'show' %>");
$('#tripInfoModal').modal('show');
moveMap($('#countryNames').text().split(', '));
removeMarkers();
addMarkers(JSON.parse($('#placesInfo').text()));